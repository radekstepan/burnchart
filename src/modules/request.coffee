#!/usr/bin/env coffee
module.exports =
    'all_milestones': (opts, cb) ->
        cb 'Not implemented'
    'all_issues': (opts, cb) ->
        # { direction: 'asc', per_page: 100 }
        cb 'Not implemented'